## roles/desktop/meta/main.yml
# Meta file for desktop 
# vim:ft=ansible:
---
## If another role lists a dependency, it will only run once by default.
allow_duplicates: no

## List of role dependencies
dependencies:
  - { role: ansible-role-repo-rpmfusion }
  # If dependency, remove brackets, list as suggested below:
  # - { role: foorole, some_parameter: 3, another_parameter: 4  }

...

